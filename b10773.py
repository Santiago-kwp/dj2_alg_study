'''
:= walrus operator 사용해보기
'''
import sys
stack = []
for _ in range(K:=int(sys.stdin.readline())):
    if i:=int(sys.stdin.readline()):
        stack.append(i)
    else:
        stack.pop()

print(sum(stack) if stack else 0)
